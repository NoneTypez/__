export enum ButtonAppearance {
  grey = "grey",
  blue = "blue",
  base = "base",
  profileDetails = "profileDetails",
}

export enum SvgIconAppearance {
  sidebarMain = "sidebarMain",
  searchInput = "searchInput",
  sortTriangle = "sortTriangle",
}

export enum CheckboxAppearance {
  tableCheckbox = "tableCheckbox",
}

export enum SearcherAppearance {
  profileSearcher = "profileSearcher",
}

export enum WindowAppearance {
  main = "main",
}

export enum SideBarButtons {
  dashboard = "dashboard",
  exchange = "exchange",
  projects = "projects",
  scripts = "scripts",
  swap = "swap",
  other = "other",
  table = "table",
}
